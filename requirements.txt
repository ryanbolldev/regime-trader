# Core ML / statistics
hmmlearn>=0.3.2
scikit-learn>=1.4.0
# Upper bounds are load-bearing: hmmlearn 0.3.x is not numpy-2 clean, and
# scipy>=1.16 hard-requires numpy>=2.0. Unpinned, pip pulled scipy 1.18 against
# numpy 1.26 and broke the whole suite ("numpy has no attribute 'long'").
numpy>=1.26.0,<2.0
pandas>=2.2.0
scipy>=1.13.0,<1.16

# Broker integration
alpaca-py>=0.26.0

# tastytrade options data (read-only chain + DXLink streaming: greeks, IV, open interest)
tastytrade>=13.0.0
truststore>=0.10.0   # validate TLS via OS trust store (local TLS-inspection proxies)

# Configuration / secrets
python-dotenv>=1.0.1

# HTML parsing (pandas.read_html backend for Wikipedia universe fetch)
lxml>=5.2.0

# Options / earnings data for wheel scanner
yfinance>=0.2.40

# Technical indicators
ta>=0.11.0           # RSI, ATR, SMA, etc.

# Dashboard
streamlit>=1.35.0
plotly>=5.22.0

# Notifications
requests>=2.32.0     # webhook delivery

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0

# Type hints
typing_extensions>=4.12.0
